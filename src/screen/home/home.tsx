import React from "react";
import { Box } from "@mui/material";
import { CardContainer } from "../../component";

const Home: React.FC = () => {
  return (
    <Box>
      <CardContainer />
    </Box>
  );
};

export default Home;
